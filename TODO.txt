-  add >> operator definition
-  add saving stream in array/pack, prepared to be sent, and than add move semantics for this pack
-  add saving stream in another representable view: some analog or extension of vector <bool> / bitset to store in bits, but show in chars '1' or '0'
-  replace cout << "Warning"; by using stderr or something like that
-  add endianness check to template that uses PutAny() and PutAnyReversed()
-  add check to bitremedy::MergeWith() for the situation where two bytes are full with BITSN == CHAR_BIT
-  check how it will work with enum type
